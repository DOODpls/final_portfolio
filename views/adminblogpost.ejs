<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel='icon' href='/images/logo/favicon.ico' type='image/x-icon'/ >
  <title>Document</title>
  <link rel="stylesheet" href="/css/admin.css">
  <link rel='icon' href='/images/logo/favicon.ico' type='image/x-icon'/ >
  <script
  src="https://code.jquery.com/jquery-3.4.1.min.js"
  integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo="
  crossorigin="anonymous"></script>
  <script src="/js/jquery.tagsinput-revisited.js"></script>
  <link rel="stylesheet" href="/css/jquery.tagsinput-revisited.css">
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.min.js" integrity="sha256-VazP97ZCwtekAsvgPBSUwPFKdrwD3unUfSGVYrahUqU=" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
  <script src="https://kit.fontawesome.com/8092725ea1.js" crossorigin="anonymous"></script>
  <link href="https://cdn.jsdelivr.net/npm/summernote@0.8.16/dist/summernote-lite.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.16/dist/summernote-lite.min.js"></script>
</head>
<body>
  <div class="admin-wrapper">
    <div class="side-menu">
      <div class="admin-div">
        <h1 class="adminheader">ADMIN</h1>
      </div>
      <a href="/admin"><button class="taboptions" id="manage-proj"><i class="fas fa-chevron-left"></i> Go Back</button>
        <div class="logout"></a>
        <a href="/logout"><p>Log Out <i class="fas fa-sign-out-alt"></i></p></a> 
      </div>
    </div>
    <div class="post-outer-cont">
        <div class="post-inner-cont">
          <div class="post-container tabcontent" id="post-container">
            <form method="POST" class="modifyblogs">
              <div>
                <legend>ID</legend>
                <input type="text" name="blogid" id="blogid" class="blogid" readonly="readonly" value="<%= blogpost.id %>" required>
              </div>
              <div>
                <legend>Title</legend>
                <input type="text" name="blogtitle" id="blogtitle" class="blogtitle" onkeyup="slugthis()" value="<%= blogpost.title %>" required>
              </div>
              <div>
                <legend>Date</legend>
                <input type="date" name="blogdate" id="blogdate" class="blogdate" value="<%= blogpost.date %>" required>
              </div>
              <div>
                <legend>Summary</legend>
                <input type="text" name="blogsumm" id="blogsumm" class="blogsumm" value="<%= blogpost.blog_summary %>" required>
              </div>
              <div>
                <legend>Slug</legend>
                <input type="text" name="blogslug" id="blogslug" class="blogslug" readonly="readonly" value="<%= blogpost.slug %>" required>
              </div>
              <div class="tagandcat">
                <div>
                  <h3>Tags</h3>
                  <input type="text" name="tagsss" id="tagsss" class="tagsss" value="<%= blogpost.tags %>">
                  <script>
                          var tagsres = "<%= tags %>"
                          var tagssa = tagsres.split(',')
                          $('#tagsss').tagsInput({

                            'autocomplete': {
                              source: tagssa
                            },
                            // min/max number of characters
                            minChars: 0,
                            maxChars: null,

                            // max number of tags
                            limit: null,

                            // RegExp
                            validationPattern: null,

                            // duplicate validation
                            unique: true
                            });
                        </script>
                </div>
                <div>
                  <legend>Category</legend>
                  <input list="category" name="category" class="category" value="<%= blogpost.category %>"/>
                    <datalist id="category" >
                      <% categories.forEach(function(items){ %>
                        <option value="<%= items %>">
                      <% }) %>
                    </datalist>
                </div>
              </div>
              <div>
                <legend>Content</legend>
                <textarea id="summernote" name="blogcontent"><%= blogpost.blog_cont %></textarea>
                <script>
                  $('#summernote').summernote({
                    placeholder: 'Hello stand alone ui',
                    tabsize: 2,
                    height: 400,
                    toolbar: [
                      ['style', ['style']],
                      ['font', ['bold', 'underline', 'clear']],
                      ['color', ['color']],
                      ['para', ['ul', 'ol', 'paragraph']],
                      ['table', ['table']],
                      ['insert', ['link', 'picture', 'video']],
                      ['view', ['fullscreen', 'codeview', 'help']]
                    ]
                  });
                </script>
              </div>
              <div>
                <input type="submit" value="Save" formaction="/admin/updated">
                <input type="submit" value="Delete" formaction="/admin/deleted">
              </div>
            </form>
          </div>
        </div>
      </div>
  </div>
</body>
<script src="/js/animations.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
<script src="http://bootstrap-tagsinput.github.io/bootstrap-tagsinput/dist/bootstrap-tagsinput.min.js"></script>
</html>